server:
  port: 8080

spring:
  application:
    name: gateway
  graphql:
    graphiql:
      enabled: false
      path: /graphiql

grpc:
  client:
    user-service:
      address: static://user-service:9091