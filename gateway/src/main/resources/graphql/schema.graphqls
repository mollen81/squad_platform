type Mutation {
    loginWithSteam(openidParamsJson: String!): AuthResponse!
}

type Query {
    # ЗАГЛУШКА
    ping: String!
}

type AuthResponse {
    userId: String!
    steamId: String!
    isNewUser: Boolean!
    token: String!
}
